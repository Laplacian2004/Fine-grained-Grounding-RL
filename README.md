# Fine-grained-Grounding-RL
This is a final project to the RL course in National Taiwan University.

# Sbert+PPO 
- Note that `train.py` uses the downloaded SentenceBert, so you should run `downloaded_sbert.py`.
- Note that the data is preprocessed to speed up the training, so you should run `data_preprocess.py`.
